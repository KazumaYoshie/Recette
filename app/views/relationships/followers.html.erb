<div class="container">
  <div class="row">
    <h2>Followers</h2>
    <div class="col-lg-7 col-md-8 mx-auto text-center">
      <% if @users.exists? %>
        <%= render 'index', users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>